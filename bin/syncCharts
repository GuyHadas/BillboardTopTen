require 'json'

charts = JSON.parse(File.read("billboard-data-11-20-2016.json"))
track_meta = JSON.parse(File.read("track-meta-11-20-2016.json"))

charts.each do |k, v|
  charts.delete(k) unless track_meta[k]
end

File.write("./public/billboard-data-11-20-2016-synced.json", JSON.generate(charts))
