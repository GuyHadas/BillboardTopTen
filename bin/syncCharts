require 'json'

charts = JSON.parse(File.read("public/charts/hiphop/billboard-data.json"))
track_meta = JSON.parse(File.read("public/charts/hiphop/previewUrls.json"))

charts.each do |k, v|
  charts.delete(k) unless track_meta[k]
end

File.write("public/charts/hiphop/charts.json", JSON.generate(charts))
